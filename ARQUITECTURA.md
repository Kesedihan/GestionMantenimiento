# 🏗️ ARQUITECTURA Y FLUJO DEL PROYECTO

## 📊 Diagrama de Componentes

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENTE (Navegador)                      │
│                                                                   │
│  http://localhost:8080/index      http://localhost:8080/login   │
└──────────────────────┬──────────────────────┬────────────────────┘
                       │                      │
                       ▼                      ▼
        ┌──────────────────────┐   ┌──────────────────────┐
        │   index.html         │   │   login.html         │
        │  (Thymeleaf)         │   │  (Thymeleaf)         │
        │                      │   │                      │
        │  - 4 opciones roles  │   │  - Email input       │
        │  - Bootstrap 5       │   │  - Password input    │
        │  - Bootstrap Icons   │   │  - Form POST /login  │
        └──────────┬───────────┘   └──────┬───────────────┘
                   │                      │
                   └──────────┬───────────┘
                              │
                              ▼
                 ┌────────────────────────┐
                 │  AuthController.java   │
                 │                        │
                 │  GET  /               │
                 │  GET  /login          │
                 │  POST /login          │
                 │  GET  /logout         │
                 │  GET  /dashboard      │
                 └──────────┬─────────────┘
                            │
                    ┌───────┴────────┐
                    │                │
                    ▼                ▼
        ┌──────────────────────┐   ┌──────────────────────┐
        │ UsuarioService.java  │   │  RolService.java     │
        │                      │   │                      │
        │ - obtenerUsuario...()│   │ - obtenerRol...()    │
        │ - validarCredenciales│   │ - crearRol()         │
        │ - crearUsuario()     │   │ - actualizarRol()    │
        │ - actualizarUsuario()│   │ - eliminarRol()      │
        └──────────┬───────────┘   └──────────┬───────────┘
                   │                          │
                   └──────────┬────────────────┘
                              │
                              ▼
                 ┌────────────────────────────────┐
                 │  PostgreSQL Database           │
                 │                                │
                 │  ┌──────┐  ┌──────┐           │
                 │  │Usuario│  │ Rol  │           │
                 │  ├───────┤  ├──────┤           │
                 │  │id_u   │  │id_r  │           │
                 │  │nombre │  │nombre│           │
                 │  │correo │  │      │           │
                 │  │pass   │  │      │           │
                 │  └───┬───┘  └──┬───┘           │
                 │      └────┬────┘               │
                 │           │                    │
                 │      ┌─────▼──────┐            │
                 │      │usuario_rol │            │
                 │      │ (M:M table)│            │
                 │      └────────────┘            │
                 └────────────────────────────────┘
```

---

## 🔄 Flujo de Autenticación

```
INICIO
  │
  ▼
┌──────────────────────────────────┐
│ Usuario accede a localhost:8080  │
└──────────────────┬───────────────┘
                   │
                   ▼
         ┌─────────────────────┐
         │ ¿Sesión activa?     │
         └──────┬─────┬────────┘
            NO  │     │ SI
                ▼     │
        ┌──────────────┘
        │
        ▼
┌─────────────────────────────────┐
│ GET /index.html                 │
│ (Página de selección de roles)  │
└──────────────────┬──────────────┘
                   │
         ┌─────────┴─────────┐
         │                   │
         ▼                   ▼
    ┌─────────┐         ┌──────────┐
    │ [Role1] │   ...   │ [Role4]  │
    └────┬────┘         └────┬─────┘
         │                   │
         └───────────┬───────┘
                     │
         (Todos redirigen a /login)
                     │
                     ▼
         ┌──────────────────────┐
         │ GET /login           │
         │ (Formulario login)   │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │ Usuario ingresa:     │
         │ - Correo             │
         │ - Contraseña         │
         │ - Click [Enviar]     │
         └──────────┬───────────┘
                    │
                    ▼
         ┌──────────────────────────┐
         │ POST /login              │
         │ @AuthController          │
         └──────────┬───────────────┘
                    │
                    ▼
         ┌──────────────────────────┐
         │ Buscar usuario en BD     │
         │ UsuarioService           │
         │ .obtenerPorCorreo()      │
         └──────────┬───────────────┘
                    │
          ┌─────────┴──────────┐
          │                    │
       EXISTE           NO EXISTE
          │                    │
          ▼                    ▼
    ┌──────────┐        ┌────────────┐
    │ Validar  │        │ Error 401  │
    │ Contrasena│       │ Redireccio │
    │           │       │ n a /login │
    └─────┬─────┘       │ con error  │
          │             └────────────┘
   ┌──────┴────────┐
   │               │
 VALIDA      INVALIDA
   │               │
   ▼               ▼
┌────────┐    ┌──────────┐
│ Guardar │    │ Error 401│
│ sesión │    │ Redirect │
│ usuario│    │ /login   │
└───┬────┘    └──────────┘
    │
    ▼
┌─────────────────────────┐
│ REDIRECT /dashboard     │
└────────────┬────────────┘
             │
             ▼
┌──────────────────────────┐
│ GET /dashboard           │
│ (Verifica sesión)        │
└────────────┬─────────────┘
             │
      ┌──────┴──────┐
      │             │
  VÁLIDA      NO VÁLIDA
      │             │
      ▼             ▼
┌──────────┐  ┌──────────┐
│dashboard.│  │ Redirect │
│html      │  │ /login   │
│Renderizar│  │          │
│con datos │  └──────────┘
│usuario   │
└─────┬────┘
      │
      ▼
┌──────────────────────────┐
│ Dashboard mostrado con:  │
│ - Nombre del usuario     │
│ - Roles asignados        │
│ - Módulos disponibles    │
│ - Botón de Logout        │
└──────────┬───────────────┘
           │
           ▼
        [Logout]
           │
           ▼
    GET /logout
       │
       ▼
    Sesión
   invalidada
       │
       ▼
   REDIRECT /
       │
       ▼
     FIN
```

---

## 📂 Flujo de Archivos

```
REQUEST /login
    │
    ├─► AuthController.login()
    │   └─► Model + templates/login.html
    │       └─► Navega a th:href="@{/login}"
    │
    └─► AuthController.procesarLogin()
        ├─► UsuarioService.obtenerUsuarioPorCorreo()
        │   └─► UsuarioRepository.findByCorreo()
        │       └─► PostgreSQL (SELECT * FROM usuario)
        │
        ├─► Validar contraseña
        │
        ├─► SI VÁLIDA
        │   ├─► session.setAttribute("usuarioActual", usuario)
        │   └─► REDIRECT /dashboard
        │
        └─► SI INVÁLIDA
            ├─► model.addAttribute("error", "...")
            └─► Renderizar templates/login.html con error
```

---

## 🔐 Capas de Seguridad

```
┌─────────────────────────────────┐
│  1. Spring Security Config      │
│  - Rutas públicas vs protegidas │
│  - CSRF protection              │
│  - Session management           │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│  2. AuthController Validation   │
│  - Verificar credenciales       │
│  - Crear sesión                 │
│  - Redirigir según resultado    │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│  3. UsuarioService Business     │
│  - Lógica de validación         │
│  - Consultas a repositorio      │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│  4. UsuarioRepository Access    │
│  - Queries a la BD              │
│  - Entidades JPA                │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│  5. PostgreSQL Database         │
│  - Almacenamiento persistente   │
│  - Validación de constraints    │
└─────────────────────────────────┘
```

---

## 🗂️ Estructura MVC/MVCS

```
MODEL (Datos)
├─ Entity/
│  ├─ Usuario.java
│  ├─ Rol.java
│  └─ ...otras entidades
│
├─ Repository/
│  ├─ UsuarioRepository.java
│  ├─ RolRepository.java
│  └─ ...otros repositorios
│
└─ Service/
   ├─ UsuarioService.java
   ├─ RolService.java
   └─ ...otros servicios

VIEW (Presentación)
├─ templates/
│  ├─ index.html (Thymeleaf)
│  ├─ login.html (Thymeleaf)
│  └─ dashboard.html (Thymeleaf)
│
└─ static/
   ├─ css/
   │  ├─ index.css
   │  └─ login.css
   └─ js/
      └─ scripts.js (opcional)

CONTROLLER (Lógica)
├─ AuthController.java
├─ ...otros controladores
│
└─ Config/
   ├─ SecurityConfig.java
   └─ WebConfig.java
```

---

## 🔄 Ciclo de Vida de una Sesión

```
Usuario NO autenticado
        │
        ▼
   LOGIN PAGE
        │
        ▼
Envía credenciales
        │
        ▼
Validación correcta
        │
        ▼
session.setAttribute("usuarioActual", usuario)
        │
        ▼
Usuario AUTENTICADO
        │
        ├─► Acceso a /dashboard ✅
        ├─► Acceso a otros recursos protegidos ✅
        │
        ▼
Click LOGOUT
        │
        ▼
session.invalidate()
        │
        ▼
Usuario NO autenticado
        │
        ▼
Redirige a /login
```

---

## 💾 Base de Datos - Relaciones

```
┌─────────────────────┐
│     USUARIO         │
├─────────────────────┤
│ id_usuario (PK)     │
│ nombre              │
│ apellido            │
│ correo (UNIQUE)     │
│ contrasena          │
│ celular             │
│ activo              │
│ descripcion_cargo   │
│ roles (N:M)         │
└────────────┬────────┘
             │
        (N:M) │
             │
    ┌────────▼────────┐
    │  USUARIO_ROL    │
    │  (Join Table)   │
    │ id_usuario (FK) │
    │ id_rol (FK)     │
    └────────┬────────┘
             │
        (N:M) │
             │
    ┌────────▼────────┐
    │      ROL        │
    ├────────────────┤
    │ id_rol (PK)    │
    │ nombre_rol     │
    │ (ENUM)         │
    └────────────────┘

Enumeración de Roles:
  - SOLICITANTE
  - OPERARIO
  - JEFE_MANTENIMIENTO
  - OFICINA_MANTENIMIENTO
  - ADMINISTRADOR
```

---

## 🌐 Rutas y HTTP Methods

```
Pública     GET   /              → index.html
Pública     GET   /login         → login.html
Pública     POST  /login         → Validar credenciales
Pública     GET   /logout        → Cerrar sesión
Protegida   GET   /dashboard     → dashboard.html (requiere sesión)

┌─────────────────────────────────────────┐
│ Rutas de Recursos Estáticos             │
├─────────────────────────────────────────┤
│ GET /css/**        → Bootstrap, Estilos │
│ GET /js/**         → Scripts            │
│ GET /images/**     → Imágenes           │
└─────────────────────────────────────────┘
```

---

## 🔗 Integración de Componentes

```
┌──────────────────────────────────────────────────────────┐
│                    SPRING APPLICATION                    │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │         SPRING SECURITY CONFIG                  │   │
│  │ - Configura rutas públicas/protegidas           │   │
│  │ - Maneja autenticación                          │   │
│  └─────────────────────────────────────────────────┘   │
│                          │                              │
│  ┌──────────────────────┴──────────────────────────┐   │
│  │                                                 │    │
│  ▼                                                 ▼    │
│ ┌──────────────────┐                  ┌────────────────┐│
│ │ AuthController   │                  │ WebConfig      ││
│ │ - Maneja URLs    │                  │ - View mapeos  ││
│ │ - Procesa datos  │                  │ - Rutas        ││
│ └────────┬─────────┘                  └────────────────┘│
│          │                                              │
│          ▼                                              │
│ ┌──────────────────────────────┐                       │
│ │  SERVICE LAYER               │                       │
│ │ - UsuarioService             │                       │
│ │ - RolService                 │                       │
│ │ (Lógica de negocio)          │                       │
│ └────────┬──────────────────────┘                      │
│          │                                              │
│          ▼                                              │
│ ┌──────────────────────────────┐                       │
│ │  REPOSITORY LAYER            │                       │
│ │ - UsuarioRepository (JPA)    │                       │
│ │ - RolRepository (JPA)        │                       │
│ │ (Acceso a datos)             │                       │
│ └────────┬──────────────────────┘                      │
│          │                                              │
│          ▼                                              │
│ ┌──────────────────────────────┐                       │
│ │  HIBERNATE/JPA               │                       │
│ │ - ORM Mapping                │                       │
│ │ - Transacciones              │                       │
│ └────────┬──────────────────────┘                      │
│          │                                              │
│          ▼                                              │
│ ┌──────────────────────────────┐                       │
│ │  POSTGRESQL DATABASE         │                       │
│ │ - Almacenamiento             │                       │
│ │ - Integridad                 │                       │
│ └──────────────────────────────┘                       │
│                                                        │
│  ┌────────────────────────────────────────────────┐   │
│  │          THYMELEAF TEMPLATE ENGINE             │   │
│  │ - Renderiza HTML dinámico                      │   │
│  │ - Procesa expresiones EL                       │   │
│  └────────────────────────────────────────────────┘   │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## 📈 Flujo de Datos Request/Response

```
USER REQUEST
    │
    ▼
┌─────────────────────────┐
│  HTTP Request           │
│  GET /login             │
│  POST /login            │
│  etc.                   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Spring DispatcherServlet           │
│  (Procesa la solicitud)             │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Controller                         │
│  (AuthController.java)              │
│  - Recibe parámetros                │
│  - Llama servicios                  │
│  - Retorna vista/datos              │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Service                            │
│  (UsuarioService.java)              │
│  - Lógica de negocio                │
│  - Llama repositorios               │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Repository                         │
│  (UsuarioRepository.java)           │
│  - Consultas JPA                    │
│  - Transacciones BD                 │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Database                           │
│  (PostgreSQL)                       │
│  - Retorna datos                    │
└────────┬────────────────────────────┘
         │
         ▼ RESPONSE
┌─────────────────────────────────────┐
│  Repository                         │
│  Retorna Entity                     │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Service                            │
│  Procesa Entity                     │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Controller                         │
│  Agrega datos a Model               │
│  Retorna nombre vista               │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  Thymeleaf                          │
│  Renderiza HTML                     │
│  Combina vista + datos              │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  HTTP Response                      │
│  Content-Type: text/html            │
│  Status: 200 OK                     │
│  Body: HTML renderizado             │
└────────┬────────────────────────────┘
         │
         ▼
      BROWSER
         │
         ▼
    Renderiza HTML
    en pantalla
```

---

## 🎯 Resumen de Arquitectura

- **Patrón**: Spring Boot MVC + MVCS con Service Layer
- **Base Datos**: PostgreSQL
- **ORM**: Hibernate JPA
- **Template Engine**: Thymeleaf
- **Seguridad**: Spring Security
- **Frontend**: Bootstrap 5 + Vanilla JS
- **Build Tool**: Maven

---

**Diagrama visual de la arquitectura general del proyecto** 🏗️
